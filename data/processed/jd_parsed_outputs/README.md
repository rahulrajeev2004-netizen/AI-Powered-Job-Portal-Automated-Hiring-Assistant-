# JD Parsed Outputs

This directory stores structured JSON files generated by `parsers/jd_parser.py`.

Each file is named `<job_id>.json` and conforms to `data/schema/schema_jd.json`.

Files here are produced by the main pipeline or by direct calls to `save_parsed_jd()`.
